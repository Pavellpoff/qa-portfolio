# Обработка изображений поста

## TC 1. Обработка отсутствующего или невалидного изображения

### Окружение
- Android Studio Emulator
- Android 13 (API 33 “Tiramisu”)
- Resolution: 2400×1800
- 480 dpi
- Proxyman

---

### Проверяемые сценарии
1. Поле `url` содержит невалидное значение
2. Поле `url` содержит пустую строку
3. Поле `url` отсутствует в response

---

### Шаги
1. Открыть пост в приложении
2. Перехватить response:
   ```http
   GET /api/v3/post?id=...
3. Изменить значение поля url согласно сценарию
4. Перезагрузить экран поста

---

### Ожидаемый результат
- приложение не падает
- интерфейс отображается корректно
- структура экрана не нарушается
- пост корректно отображается без изображения или с placeholder

---

### Фактический результат

Во всех сценариях:
- приложение работает стабильно
- структура интерфейса не нарушена
- пост отображается корректно

---

### Наблюдения

При невалидном URL отображается текст: failed to load media 🙁

При пустом или отсутствующем поле url пост отображается без изображения


---
# Обработка заголовков поста

## TC 2. Длинный заголовок поста

### Окружение
- Android Studio Emulator
- Android 13 (API 33 “Tiramisu”)
- Resolution: 2400×1800
- 480 dpi
- Proxyman

---

### Шаги
1. Перехватить response:
   ```http
   GET /api/v3/post?id=...
2. Заменить значение поля name на длинный текст
3. Перезагрузить экран поста

---

### Ожидаемый результат
- приложение не падает
- длинный заголовок корректно переносится
- текст не выходит за границы экрана
- элементы интерфейса не перекрываются
- структура интерфейса не нарушена

---

### Фактический результат
- приложение работает стабильно
- длинный заголовок корректно отображается
- структура интерфейса не нарушена
- визуальных дефектов не обнаружено

---

### Вывод

Приложение корректно обрабатывает длинный заголовок поста без нарушения структуры интерфейса.




## TC 3. Отсутствующий заголовок поста

### Окружение
- Android Studio Emulator
- Android 13 (API 33 “Tiramisu”)
- Resolution: 2400×1800
- 480 dpi
- Proxyman

---

### Шаги
1. Перехватить response: GET /api/v3/post?id=...
2. Удалить значение поля name
3. Перезагрузить экран поста

---

### Ожидаемый результат
- приложение не падает
- экран отображается корректно
- структура интерфейса не нарушена
- пост корректно отображается без заголовка

---

### Фактический результат
- приложение работает стабильно
- пост отображается без заголовка
- структура интерфейса не нарушена

---

### Вывод

Приложение корректно обрабатывает отсутствие заголовка поста без нарушения работы интерфейса.

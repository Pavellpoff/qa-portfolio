# Денежные переводы (API)

## Описание

Данный набор API-автотестов проверяет работу эндпоинта создания денежных переводов между пользователями.

Основное внимание уделено проверке бизнес-логики создания транзакций, обработке некорректных входных данных, исследованию граничных значений и анализу поведения API в нестандартных сценариях.

---

## Покрываемые сценарии

| №  | Сценарий                                                        | Статус |
| -- | --------------------------------------------------------------- | :----: |
| 1  | Успешное создание новой транзакции                              | ✅ |
| 2  | Создание транзакции с пустым полем Amount                       | ✅ |
| 3  | Создание транзакции с пустым Description                        | ✅ |
| 4  | Создание транзакции с Description, состоящим только из пробелов | ✅ |
| 5  | Создание транзакции с очень большим значением Amount            | ✅ |
| 6  | Создание транзакции с отрицательным значением Amount            | ✅ |
| 7  | Создание транзакции с дробной суммой (55.33)                    | ✅ |
| 8  | Создание транзакции с дробной суммой (55.01)                    | ✅ |
| 9  | Создание транзакции с дробной суммой (55.99)                    | ✅ |
| 10 | Создание транзакции для несуществующего получателя              | ✅ |
| 11 | Попытка создания транзакции самому себе                         | ✅ |
| 12 | Создание транзакции с amount = 0                       | ❌ |
| 13 | Создание транзакции с  amount = null                   | ❌ |


---

## Проверки

### 1. Создание новой транзакции

Проверяется:

* успешная авторизация пользователя;
* получение идентификатора отправителя;
* получение идентификатора получателя;
* успешное создание новой транзакции;
* корректность данных созданной транзакции.

---

### 2. Проверка обязательных полей

Проверяется обработка следующих сценариев:

* пустое значение `amount`;
* пустое значение `description`.

---

### 3. Проверка граничных и нестандартных значений

Проверяется обработка:

* отрицательных значений суммы;
* очень больших значений суммы;
* дробных значений суммы;
* строки, состоящей только из пробелов, в поле `description`.

---

### 4. Проверка бизнес-логики

Проверяется:

* создание транзакции несуществующему пользователю;
* попытка создания транзакции самому себе.

---

## Проверяемые HTTP-методы

| Метод | Назначение                     |
| ----- | ------------------------------ |
| POST  | Авторизация пользователя       |
| GET   | Получение списка пользователей |
| POST  | Создание новой транзакции      |
| GET   | Получение списка транзакций    |

---

## Проверяемые HTTP-статусы

| Код | Значение                                               |
| --- | ------------------------------------------------------ |
| 200 | Запрос успешно выполнен                                |
| 422 | Ошибка валидации входных данных                        |
| 500 | Внутренняя ошибка сервера (исследовательский сценарий) |

---

## Особенности реализации

Автотесты не ограничиваются проверкой HTTP-статусов и структуры ответа API.

Дополнительно исследуется поведение системы при передаче некорректных данных, анализируется обработка граничных значений, проверяется соблюдение бизнес-правил, а также выявляются различия между ожидаемым и фактическим поведением API.

Несколько тестов используются для подтверждения потенциальных дефектов серверной логики и последующего оформления баг-репортов.

---

## Расположение автотестов

`tests/paveltests/API/transactionAPI.spec.js`
